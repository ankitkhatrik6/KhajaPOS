{
  "name": "restaurant-pos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "php artisan serve --host=0.0.0.0 --port=3000",
    "build": "php artisan --version",
    "start": "php artisan serve --host=0.0.0.0 --port=3000",
    "lint": "php -l routes/web.php"
  }
}
